/* Operations-only rendering. All Stripe reads are authorized server-side. */
function stripeInsights(){
 const attempts=work.orders.flatMap(o=>(o.stripeEvidence||[]).map(c=>({...c,order:o})));
 const blocked=attempts.filter(c=>c.type==='blocked'),disputed=attempts.filter(c=>c.disputed);
 return workspaceTop('Payments + Radar + Connect','Your Stripe evidence, connected.','Follow FoodNow payment attempts, risk decisions and partner balances in the Stripe test account powering this application.')+
 `<section class="panel"><div class="row"><div><h2>Stripe account connection</h2><p class="meta">Verify the account behind the server’s test key. Secret keys never appear here.</p></div><button class="button" id="verify-stripe-account">Check account connection</button></div><div id="stripe-account-result" class="spacer">${config.mode==='rehearsal'?notice('Rehearsal is active. No Stripe account is connected to this runtime. Configure .env and restart in Stripe mode.','warn'):'Account identity has not been checked yet.'}</div></section>
 <div class="restaurant-summary-grid spacer"><article class="panel"><span class="eyebrow">01 · Payments</span><h3>Collect and confirm</h3><p>Payment Element and Express Checkout collect payment details directly with Stripe. PaymentIntents track attempts; signed webhooks release paid orders.</p></article><article class="panel"><span class="eyebrow">02 · Radar</span><h3>Evaluate transaction risk</h3><p>Stripe evaluates eligible payment attempts using the account’s Radar settings. FoodNow retrieves Stripe outcomes; no separate FoodNow fraud score is generated.</p></article><article class="panel"><span class="eyebrow">03 · Connect</span><h3>Onboard and distribute</h3><p>Separate restaurant and courier connected accounts receive transfers after delivery. Transfer IDs reconcile earnings; a bank payout is a later, separate movement.</p></article></div>
 <div class="section-heading"><div><h2>Payment and risk evidence</h2><p>Explicitly refreshed snapshots of up to 100 attempts per order. Counts below cover fetched FoodNow attempts only.</p></div><button class="button secondary" id="refresh-stripe-evidence" ${config.mode==='rehearsal'?'disabled':''}>Refresh Stripe evidence</button></div><div id="stripe-evidence-progress" role="status"></div>
 <div class="metric-grid">${metric('Fetched attempts',attempts.length,'Not account-wide analytics')}${metric('Blocked outcomes',blocked.length,'Stripe outcome type: blocked')}${metric('Disputed charges',disputed.length,'Current Stripe charge flag')}</div>
 <div class="table-wrap"><table><thead><tr><th>FoodNow order / Stripe attempt</th><th>Payment</th><th>Radar outcome</th><th>Risk</th><th>Evidence</th></tr></thead><tbody>${attempts.map(c=>`<tr><td><button data-inspect="${c.order.id}">${escapeHtml(c.order.restaurantName)}</button><small class="table-sub mono">${escapeHtml(c.id)}</small></td><td>${escapeHtml(c.status)} · ${money(c.amount)}<small class="table-sub">${escapeHtml(c.method||'Method unavailable')}</small></td><td>${escapeHtml(c.type||'Not provided')}<small class="table-sub">${escapeHtml(c.sellerMessage||c.failureMessage||c.reason||'No additional message')}</small></td><td>${escapeHtml(c.riskLevel||'Not provided')}<small class="table-sub">Score: ${c.riskScore==null?'Not provided':escapeHtml(c.riskScore)}${c.disputed?' · Disputed':''}${c.refunded?' · Refunded':''}</small></td><td><a class="text-button" target="_blank" rel="noopener" href="https://dashboard.stripe.com/test/payments/${encodeURIComponent(c.order.paymentIntentId)}">Open in Stripe ↗</a><small class="table-sub">Fetched ${date(c.order.evidenceCheckedAt)}</small></td></tr>`).join('')||'<tr><td colspan="5">No Stripe evidence fetched. Run test payments, including a Radar test scenario, then refresh. Rehearsal never creates Stripe risk evidence.</td></tr>'}</tbody></table></div>
 <section class="panel spacer"><h2>Restaurant and courier connected accounts</h2><p class="meta">FoodNow approval is separate from Stripe requirements. These balances belong to partners in the same platform integration.</p><div class="table-wrap"><table><thead><tr><th>Partner</th><th>Role</th><th>Account</th><th>Transfers</th><th>Bank payouts</th></tr></thead><tbody>${[...work.restaurants.map(p=>({...p,kind:'Restaurant'})),...work.couriers.map(p=>({...p,kind:'Courier'}))].map(p=>`<tr><td>${escapeHtml(p.name)}</td><td>${p.kind}</td><td class="mono">${escapeHtml(p.stripeAccount||'Onboarding not started')}</td><td>${config.mode==='rehearsal'?'Simulated':p.transfersReady?'Active':'Not active'}</td><td>${p.payoutsEnabled?'Enabled':'Not enabled'}</td></tr>`).join('')}</tbody></table></div></section>
 <section class="panel spacer"><h2>Presentation test scenarios</h2><p>Use these only inside the real Stripe test Payment Element. Future expiry and any three-digit test CVC. A blocked payment is not proof of actual fraud.</p><div class="money-flow"><div><small>Success</small><strong class="test-card">4242 4242 4242 4242</strong></div><div><small>Issuer decline</small><strong class="test-card">4000 0000 0000 0002</strong></div><div><small>Radar always blocked</small><strong class="test-card">4100 0000 0000 0019</strong></div></div><p class="note">Risk scores and detailed controls depend on product availability and account access. Missing data is shown as unavailable. Test data demonstrates behavior, not measured fraud reduction.</p><a class="text-button" href="https://docs.stripe.com/testing#fraud-prevention" target="_blank" rel="noopener">Stripe test scenarios ↗</a></section>
 <section class="panel spacer"><h2>NEXT · Billing and data</h2><p>FoodNow+ remains a Billing preview. The local order journal and these API snapshots are working; a durable event bus, Stripe Data Pipeline and warehouse analytics remain planned extensions. Use the verified Stripe account’s Dashboard for available account-wide reporting.</p><a class="button secondary" href="#roadmap">Explore the roadmap →</a></section>`;
}
async function verifyStripeAccount(){const b=$('#verify-stripe-account');b.disabled=true;try{const r=await api('/api/stripe/account');$('#stripe-account-result').innerHTML=r.connected?notice(`<strong>${escapeHtml(r.account.name||'Stripe platform account')}</strong><br>Account: <span class="mono">${escapeHtml(r.account.id)}</span> · ${escapeHtml(r.account.country)} · Test API<br>Charges: ${r.account.chargesEnabled?'enabled':'not enabled'} · Bank payouts: ${r.account.payoutsEnabled?'enabled':'not enabled'}<br><a class="text-button" target="_blank" rel="noopener" href="https://dashboard.stripe.com/${encodeURIComponent(r.account.id)}/test/dashboard">Open this Stripe account ↗</a><p class="note">Account identity is verified by the server’s secret key. The checkout also requires the matching publishable key and a working signed webhook listener.</p>`):notice(escapeHtml(r.message),'warn')}catch(e){$('#stripe-account-result').innerHTML=notice(escapeHtml(e.message),'warn')}finally{b.disabled=false}}
async function refreshStripeEvidence(){const b=$('#refresh-stripe-evidence');b.disabled=true;const orders=work.orders.filter(o=>o.paymentIntentId);let complete=0,failed=0;for(const o of orders){$('#stripe-evidence-progress').textContent=`Fetching Stripe evidence ${complete+failed+1} of ${orders.length}…`;try{await api('/api/orders/'+o.id+'/stripe-evidence',{});complete++}catch{failed++}}await route(true);toast(`Refreshed ${complete} orders.${failed?' '+failed+' could not be refreshed; existing snapshots retained.':''}`)}
